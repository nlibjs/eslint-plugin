import * as printFileName from './print-filename.js';
import * as restrictGlobals from './restrict-globals.js';

export const rules = {
    'print-filename': printFileName,
    'restrict-globals': restrictGlobals,
};
